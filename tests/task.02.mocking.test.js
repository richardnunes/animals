describe("when the getAll() storage helper is called", () => {
  it("should return all the items in animals local storage", () => {});
});

describe("when the addToLocalStorage() storage helper is called", () => {
  it("should add a single entry to the animals local storage", () => {});
});

describe("when the removeFromLocalStorage() storage helper is called", () => {
  it("should remove a single entry from the animals local storage", () => {});
});

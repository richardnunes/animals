describe("when the like button is clicked", () => {
  it("should render a new entry in the likes saved list", () => {});
});

describe("when the remove button is clicked", () => {
  it("should call the remove item function correctly", () => {});
});
